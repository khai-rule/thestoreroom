export const showCreatePopUp = (modal: any) => ({
    type: "SHOW_MODAL",
    payload: modal
  });
  
  export const hideCreatePopUp = (modal: any) => ({
    type: "HIDE_MODAL",
    payload: modal
  });
  
